Borrow(&i32 ex)
{
	ex = ex * 5 + 4;	
}

Copy(i32 ex)
{
	ex = ex * 5 + 4;	
}

Main() -> bool
{
	i32 ex = 8;
	Printf("Initial : {ex}\n");
	Copy(ex);
	Printf("Copy    : {ex}\n");
	Borrow(ex);
	Printf("Borrow  : {ex}\n");

	return true;
}
