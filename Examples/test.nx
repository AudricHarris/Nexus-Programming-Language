/![WARN_UNUSED]!/
/![NO_WARNINGS]!/
Main()
{
	let[] t = new char[5];
	
	let cpt = 0;
	while (cpt < 5)
	{
		cpt++;
	}
	cpt = 0;
	let tmp = 'a';
	while (cpt < 5)
	{
		Printf("{tmp}\n");
		cpt++;
	}
	i32 c = 9;
}
